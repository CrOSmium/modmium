## Arch Linux:
```bash
yay -S --needed curl wget jq 7zip util-linux coreutils vboot-utils
```

## Debian (requires sid repo)
```bash
sudo apt update
sudo apt install -y curl wget p7zip-full util-linux coreutils sed vboot-utils
```
